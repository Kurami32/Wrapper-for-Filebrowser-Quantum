<resources>
    <style name="Base.Theme.FileBrowserQuantum" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/cyan</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/black</item>
        <!-- Text colors for dark mode -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/gray</item>
        <!-- Button styles -->
        <item name="buttonStyle">@style/Widget.FileBrowserQuantum.Button.Night</item>
    </style>

    <!-- Button style for dark theme -->
    <style name="Widget.FileBrowserQuantum.Button.Night" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/button_text_dark</item>
        <item name="android:backgroundTint">?attr/colorPrimary</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <style name="Theme.FileBrowserQuantum" parent="Base.Theme.FileBrowserQuantum" />
</resources>